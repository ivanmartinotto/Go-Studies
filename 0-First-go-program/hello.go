package main

import "fmt"

func main() {
	fmt.Println("Hello, World!")
}

/* Commands Go Toolchain
go run: runs the Go program
go build: compiles the Go program into an executable
go test: runs tests in the Go program
go fmt: formats the Go code according to standard style guidelines
go get: manages dependencies and retrieves packages
go install: installs the Go program or package
*/
